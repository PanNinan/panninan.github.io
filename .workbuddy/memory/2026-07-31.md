# 2026-07-31

## GitHub Trending 自动博客项目 — 设计决策
- 目标：Hexo + GitHub Pages 托管，每日抓取 github.com/trending 生成「当日热门项目」博文并推送。
- 运行方式：本地定时任务（Windows 任务计划程序），非 GitHub Actions。
- 内容深度：纯数据聚合（无 AI），抓取 GitHub 原始字段。
- 语言：中文为主（中文 UI + 中文标签，项目原描述保留英文）。
- 筛选标准：在指定语言集合内，按「当日 star 增量」降序取 Top N（用户选了选项 1+2 的组合）。
- 待确认：目标语言列表、Top N 数值、每日运行时间、主题选择。
- 现状：Hexo 7.x 脚手架，landscape 主题，language=en，deploy.type 为空；尚未 git init，无内容。
- 计划：分阶段推进 — 仓库准备 → 抓取脚本(Python) → 博文模板 → 构建部署 → 本地定时任务 → 主题优化 → 监控维护。

## 参数已确认（2026-07-31 第二轮）
- 目标语言集合：Python、Go、Rust、TypeScript；额外加「AI Agent 精选」桶（非语言，走 GitHub Search API `topic:ai-agent`，按总星标 Top 3）。
- Top N：每语言取 3，合并后按 owner/repo 去重；博文分「编程语言热门(今日星标)」与「AI Agent 精选(总星标)」两区。
- 运行时间：每日 18:00（北京时间）。
- 主题：先保留 landscape，后续可能换 cactus。
- 已落地：PLAN.md 计划文档；git init（main 分支）；_config.yml 改 language=zh-CN、url=https://panninan.github.io/、deploy=git→gh-pages；hexo-deployer-git 后台安装中。
- 待用户配合：建 GitHub 仓库(panninan.github.io 用户页 or 项目页)、配 SSH/PAT 免交互 push、复核 AI Agent 处理方式、GitHub Pages 设 gh-pages 源。
